{
  "name": "openaigame1",
  "version": "0.3.0",
  "private": true,
  "description": "Development tooling for the ECHO BREACH HTML5 game",
  "scripts": {
    "format": "prettier --write \"echo-breach/**/*.{html,css,js,json,md}\"",
    "format:check": "prettier --check \"echo-breach/**/*.{html,css,js,json,md}\"",
    "test": "node --test echo-breach/tests/*.test.js",
    "release:check": "node scripts/verify-release.mjs",
    "check": "npm run format:check && npm test && node --check echo-breach/game-balance.js && node --check echo-breach/monster-data.js && node --check echo-breach/room-data.js && node --check echo-breach/echo-core.js && node --check echo-breach/game.js && npm run release:check"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
